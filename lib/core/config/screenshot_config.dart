/// Configuration for screenshot mode
/// Set [isScreenshotMode] to true to hide all ads for clean screenshots
class ScreenshotConfig {
  /// Set this to true to temporarily hide all banner ads for screenshots
  /// Remember to set back to false before releasing
  static const bool isScreenshotMode = false; // Set to true for screenshots, false for normal operation
}